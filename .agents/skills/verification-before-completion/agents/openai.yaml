interface:
  display_name: "Verification Before Completion"
  short_description: "Verify changes with sufficient evidence"
  default_prompt: "Use $verification-before-completion to gather the cheapest sufficient evidence for these changes."

policy:
  allow_implicit_invocation: true
