namespace DigBlocks.Client.Flow
{
    public enum ApplicationState
    {
        Launching,
        Intro,
        Title,
        Loading,
        Playing,
        Leaving,
        ShuttingDown
    }
}
